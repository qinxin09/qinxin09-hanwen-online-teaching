server:
  port: 8300
spring:
  servlet:
    multipart:
      max-file-size: 1MB #限制上传的文件大小
  datasource: # 数据库的数据配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    # url不用换行，由于数据太长了这里换行了
    url: jdbc:mysql://localhost:3306/hanwenonline?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: 123456
  application:
    name: springcloud-resource-manage-8300
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

## mybatis 配置
mybatis:
#  mapper-locations: classpath:mapper/custom/*.xml # 扫描mybatis的实现dao接口*.xml文件
  mapper-locations: classpath*:mapper/**/*.xml # 扫描mybatis的实现dao接口*.xml文件